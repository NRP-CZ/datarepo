# Definition of record for datasets. Please do not add ccmm model here,
# add the ccmm_preset instead.
Record:
  type: object
  properties:
    files:
      type: object
      label:
        cs: Soubory
        en: Files
      properties:
        enabled:
          type: boolean
          label:
            cs: Povolit soubory
            en: Enable files
