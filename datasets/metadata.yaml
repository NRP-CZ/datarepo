# Definition of metadata for datasets. Please do not add ccmm model here,
# add the ccmm_preset instead.

# add the ccmm_preset instead.
Metadata:
  type: object
  properties:
    title:
      required: true
